# =============================================================================
# ENVIRONMENT CONFIGURATION EXAMPLE
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to Git (it's already in .gitignore)
#
# 📚 LEARNING NOTE:
# Environment variables are used to store sensitive data (API keys, passwords)
# and configuration that changes between environments (dev, staging, production)
#
# In Next.js:
# - NEXT_PUBLIC_* variables are exposed to the browser (client-side)
# - Variables without NEXT_PUBLIC_ are only available on the server
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Supabase is our PostgreSQL database provider
# Get these values from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase project URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anon/Public key - safe for client-side use (has Row Level Security)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key_here

# Service Role key - NEVER expose to client! Only use in API routes
# This key bypasses Row Level Security - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_key_here

# -----------------------------------------------------------------------------
# BLOCKCHAIN CONFIGURATION (for future implementation)
# -----------------------------------------------------------------------------
# These will be used when we connect to Hedera blockchain

# Network: 'testnet' or 'mainnet'
NEXT_PUBLIC_HEDERA_NETWORK=testnet

# Your deployed smart contract address
NEXT_PUBLIC_CONTRACT_ADDRESS=0.0.123456

# -----------------------------------------------------------------------------
# IPFS CONFIGURATION (optional - for document storage)
# -----------------------------------------------------------------------------
# IPFS gateway for retrieving files
NEXT_PUBLIC_IPFS_GATEWAY=https://ipfs.io/ipfs/

# Pinata credentials (if using Pinata for IPFS pinning)
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_KEY=your_pinata_secret_key

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
# Your app's base URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API rate limiting (requests per minute)
API_RATE_LIMIT=100
